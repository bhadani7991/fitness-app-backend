const saltRound = 10;
const cookieTimeoutDuration = 6 * 60 * 60 * 1000;
const chunkSize = 5;
const noOfWeek = 4;
const fromEamilID = "awsbhadani1997@gmail.com";

module.exports = {
  saltRound,
  cookieTimeoutDuration,
  chunkSize,
  noOfWeek,
  fromEamilID,
};
